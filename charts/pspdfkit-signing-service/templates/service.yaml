apiVersion: v1
kind: Service
metadata:
  name: {{ include "pspdfkit-signing-service.fullname" . }}
  labels:
    {{- include "pspdfkit-signing-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pspdfkit-signing-service.selectorLabels" . | nindent 4 }}

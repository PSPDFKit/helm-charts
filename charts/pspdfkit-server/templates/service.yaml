apiVersion: v1
kind: Service
metadata:
  name: {{ include "pspdfkit-server.fullname" . }}
  labels:
    {{- include "pspdfkit-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: api
      protocol: TCP
      name: api
  selector:
    {{- include "pspdfkit-server.selectorLabels" . | nindent 4 }}

config:
  port: 4000
  documentEngineUrl: http://document-engine:5000

database:
  postgres:
    host: postgres
    port: 5432
    database: ai-assistant
    username: postgres
    password: testpassword

gateway:
  enabled: true
  annotations:
    test-annotation: value
  labels:
    test-label: value
  parentRefs:
    - name: my-gateway
      namespace: gateway-infra
      sectionName: https
  hostnames:
    - ai-assistant.example.com

apiVersion: v1
kind: Service
metadata:
  name: {{ include "pspdfkit-processor.fullname" . }}
  labels:
    {{- include "pspdfkit-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: api
      protocol: TCP
      name: api
  selector:
    {{- include "pspdfkit-processor.selectorLabels" . | nindent 4 }}

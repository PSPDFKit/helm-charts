apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "maestrod.fullname" . }}
  labels:
    {{- include "maestrod.labels" . | nindent 4 }}
data:
  NUTRIENT_SHOW_SCALAR: {{ .Values.maestro.showScalar | quote }}
  NATIVESDK_VISION_LOGS: {{ .Values.maestro.nativeSdkVisionLogs | quote }}
